// import React from 'react';
// import { render, fireEvent } from '@testing-library/react';
// import Quiz from './Quiz';
// import { mockQuizData } from './mockQuizData';

describe('Quiz Component', () => {

    test('renders quiz component with questions', () => {
        // const { getByTestId } = render(<Quiz quizData={mockQuizData} />);
        // expect(getByTestId(DATA_TEST_IDS.QUIZ_TITLE)).toBeInTheDocument();
        // expect(getByTestId(DATA_TEST_IDS.QUIZ_ANSWER)).toBeGreaterThan(0)
    });

    test('allows user to select answers', () => {
        // Arrange
        // const { getByText } = render(<Quiz quizData={mockQuizData} />);

        // Act
        // const firstOption = getByText(mockQuizData.questions[0].options[0].text);
        // fireEvent.click(firstOption);

        // Assert
        // expect(firstOption).toBeChecked();
    });

});
